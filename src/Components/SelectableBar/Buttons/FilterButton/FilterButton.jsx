import './Filter.css';

const FilterButton = () => {
  return (
    <div className='filter-container'>
      <span className='filter-link'>Filter</span>
    </div>
  );
};

export default FilterButton;
